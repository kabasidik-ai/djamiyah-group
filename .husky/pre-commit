#!/bin/sh
# Pre-commit hook — djamiyah-group
# Vérifie le lint avant chaque commit

echo "🔍 Pre-commit: running lint-staged..."
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Lint failed. Fix errors before committing."
  exit 1
fi

echo "✅ Pre-commit checks passed."
